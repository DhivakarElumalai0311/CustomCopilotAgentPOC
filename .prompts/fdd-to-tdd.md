# FDD → TDD Prompt Template

You will receive an FDD JSON with fields: id, title, body.
Produce a Test Design Document (TDD) in Markdown with:
1. A one-line summary of the feature.
2. Acceptance criteria (3 bullets).
3. Test cases (2 simple test cases with steps).

Format strictly as Markdown. Keep it short.
